apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: python-app
  description: Sample Flask service exposing info and health endpoints, deployed to Kubernetes via Helm
  # Example for optional annotations
  annotations:
    github.com/project-slug: faisalcodesinfrastructure/python-app
    backstage.io/techdocs-ref: dir:./docs

spec:
  type: service
  owner: development
  lifecycle: experimental